# MACRO RECORD — compare with the current working printer before installation.

# The working setup includes:
# - START_PRINT: heat, home, wait for target temperatures, then lay two purge lines.
# - LOAD_FILAMENT, UNLOAD_FILAMENT and RETRACT_FILAMENT: heat before movement and
#   place the toolhead at an accessible front-centre position.
# - MOVE_BED_CENTER: home if required, lift Z safely and move to bed centre for
#   manual Z adjustment.
# - Screw-based manual bed levelling because no probe is installed.

# TODO: Copy the tested macro definitions from the printer and remove anything
# private or machine-specific before public release.

